[project]
name = "optimal-round-ai"
version = "0.1.0"
description = "AI / LLM / agentic / generative AI practice problems, graded offline"
requires-python = ">=3.11"
dependencies = [
  "numpy>=2.0",
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
